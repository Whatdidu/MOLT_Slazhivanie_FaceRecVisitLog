# 🛰️ Sputnik Face ID: MVP Internal Office Access

**Репозиторий:** [https://github.com/sputnik-systems/AIGellingMOLT](https://github.com/sputnik-systems/AIGellingMOLT)

## 📖 Контекст проекта
**Компания:** «Спутник» (разработка умных домофонов и камер).
**Задача:** Создание системы распознавания лиц для контроля посещаемости внутреннего офиса.
**Входные данные:** Камера пишет видео 24/7. Отдельный компонент извлекает кадры по событиям (движение/кнопка) и передает их в нашу систему.
**Масштаб:** Десятки сотрудников (до 100). Срок MVP — 1 месяц.

---

## 🏗️ Архитектура и Принципы

### Ключевые принципы разработки:
- **Минимум микросервисов:** Модульный монолит на Python + FastAPI.
- **Безопасность PII (Персональные данные):** - Храним **embeddings** (векторы) вместо сырых фото.
    - Оригиналы фото — только для отладки с автоматическим удалением (TTL).
- **Надежность:** Обязательное логирование всех событий и ошибок распознавания.
- **Интерфейс решения:** Статусы `match`, `unknown`, `low_confidence` с указанием уверенности (confidence).
- **Инфраструктура:** Полная воспроизводимость через Docker Compose + PostgreSQL.

### Схема компонентов:
```ascii
[Камера 24/7] 
      |
[Snapshot Extractor] --(Image)--> [ FastAPI (Татьяна) ]
                                      |
          +---------------------------+---------------------------+
          |                           |                           |
[Recognition (Мансур)]      [Attendance (Лиля)]        [Employees (Ольга)]
- ML-инференс              - Журнал посещений          - CRUD API
- Векторизация             - Бизнес-фильтры            - Enrollment
- Сравнение векторов       - Веб-админка               - БД схемы
- Пороги качества          - Отчеты и экспорт          - TTL управление
```

---

## 👥 Команда и Зоны ответственности

### 1. Мансур — Архитектор и ML-лид (Максимальная нагрузка)
**Модуль:** `app/modules/recognition.py`

**Ответственность:**
- **ML-инференс:** Настройка DeepFace/ArcFace и запуск на CPU в Docker
- **Математика сравнения:** Написание функции поиска самого похожего вектора в базе (Cosine Similarity)
- **Качество и пороги:** Определение уровней уверенности (match, unknown, low_confidence)
- **Оптимизация:** Обеспечение быстрого распознавания без перегрузки сервера

### 2. Лиля — Сервис учёта и Frontend (Нагрузка +)
**Модули:** `app/modules/attendance.py` + Веб-админка

**Ответственность:**
- **Журнал посещаемости:** Логика записи каждого входа в БД
- **Бизнес-фильтры:** Реализация правила «анти-спам» (не писать лог каждые 2 секунды)
- **Веб-админка:** Единый интерфейс для просмотра посещаемости, управления сотрудниками, загрузки фото
- **Отчёты:** Выгрузка посещаемости за период (Excel/JSON) для HR/Админа

### 3. Ольга — Backend и БД (Нагрузка +)
**Модули:** `app/modules/employees.py`, `app/db/models.py`, `app/db/session.py`

**Ответственность:**
- **БД схемы:** Создание SQLAlchemy/SQLModel моделей (employees, attendance_log, embeddings)
- **Миграции:** Настройка Alembic или ручных скриптов миграций
- **CRUD API:** REST API для управления сотрудниками (`POST /api/v1/employees/`)
- **Enrollment:** Процесс регистрации (загрузка фото → вызов функции Мансура → сохранение вектора)
- **Управление фото:** Реализация логики автоматического удаления старых снапшотов (TTL 7 дней)

### 4. Татьяна — Интеграция и Infrastructure (Нагрузка -)
**Модули:** `app/main.py`, `app/core/`

**Ответственность:**
- **Приемный шлюз:** Настройка FastAPI эндпоинта, который принимает POST от камеры и парсит картинку
- **Роутинг:** Структура приложения, подключение модулей как роутеров
- **Валидация потока:** Базовая проверка размера/качества изображения
- **Infrastructure:** Настройка Docker-портов, проверка сетевой связности
- **Core:** Конфигурация (.env), логгер, константы, Trace ID (сквозной ID для каждого кадра)


---

## 🌿 Git Branches

Каждый участник работает в своей feature-ветке:

| Ветка | Ответственный | Описание |
|-------|---------------|----------|
| `feature/infrastructure` | Татьяна | FastAPI каркас, Docker, конфигурация |
| `feature/database` | Ольга | Модели БД, миграции, CRUD API |
| `feature/ml-recognition` | Мансур | ML-модели, векторизация, распознавание |
| `feature/attendance` | Лиля | Журнал посещений, бизнес-логика |
| `feature/admin-ui` | Лиля | Веб-админка, интерфейс управления |

### Workflow
1. Работаем в своей ветке
2. Создаём PR в `master` с описанием изменений
3. Code review от другого участника
4. Merge после одобрения

---

## 🔧 Технические спецификации

### 1. Стек
- **Язык:** Python 3.10+
- **Фреймворк:** FastAPI
- **БД:** PostgreSQL (как наиболее надежная для хранения векторов в массивах)
- **Deployment:** Docker Compose

### 2. Инфраструктура деплоя

| Компонент | Платформа | Описание |
|-----------|-----------|----------|
| **Веб-админка (Frontend)** | GitHub Pages | Статический сайт с интерфейсом управления |
| **API (Backend)** | VPS сервер | FastAPI приложение в Docker |
| **База данных** | VPS сервер | PostgreSQL в Docker контейнере |

```
┌─────────────────┐      ┌─────────────────────────────────┐
│  GitHub Pages   │      │         VPS Server              │
│  (Frontend)     │ ───► │  ┌─────────┐    ┌────────────┐  │
│  Веб-админка    │      │  │ FastAPI │────│ PostgreSQL │  │
└─────────────────┘      │  │  :8000  │    │   :5432    │  │
                         │  └─────────┘    └────────────┘  │
                         └─────────────────────────────────┘
```

> **Примечание:** Доступы к VPS серверу будут переданы участникам команды по мере необходимости.

### 3. Политика хранения данных
- **Фото сотрудников:** Хранятся только до момента генерации вектора
- **Снапшоты с камеры:** Хранятся в `/static/debug_photos` с TTL 7 дней (авто-удаление)
- **Логи:** Все запросы пишутся в таблицу `attendance_log` с `trace_id`

### 4. API Контракты
- `POST /api/v1/recognition/detect` — Принимает кадр, возвращает `person_id` и `confidence`
- `POST /api/v1/employees/` — Регистрация сотрудника (ФИО + Фото -> Вектор)
- `GET /api/v1/attendance/status` — Список сотрудников со статусом "в офисе/ушел"

---

## 📁 Структура проекта

```text
.
├── app/
│   ├── main.py              # Точка входа, FastAPI роутинг (Татьяна)
│   ├── core/                # Конфиг (.env), логгер, константы, Trace ID (Татьяна)
│   ├── modules/
│   │   ├── recognition.py   # Обработка изображений и ML (Мансур)
│   │   ├── attendance.py    # Логика посещений и журналы (Лиля)
│   │   └── employees.py     # CRUD API сотрудников и Enrollment (Ольга)
│   ├── db/
│   │   ├── session.py       # Подключение к PostgreSQL (Ольга)
│   │   └── models.py        # SQLAlchemy/SQLModel схемы (Ольга)
│   ├── static/              # Хранилище снапшотов (TTL 7 дней)
│   └── templates/           # Шаблоны веб-админки (Лиля)
├── docker-compose.yml       # Оркестрация (App + DB)
├── Dockerfile               # Сборка образа (Python + ML-libs)
├── requirements.txt         # Зависимости (fastapi, deepface, sqlalchemy, psycopg2-binary)
└── .env.example             # Пример настроек
```

---

## 📅 План работ (4 недели)

**Неделя 1:** 
- Настройка интеграции с камерой и каркас FastAPI (Татьяна)
- Создание схем БД и миграций (Ольга)
- Выбор ML-моделей на Hugging Face (Мансур)
- Начало работы над веб-админкой (Лиля)

**Неделя 2:** 
- Реализация векторизации и сравнения векторов (Мансур)
- CRUD API для сотрудников и Enrollment (Ольга)
- Разработка веб-админки (Лиля)

**Неделя 3:** 
- Бизнес-логика журнала посещений и фильтрации дублей (Лиля)
- Интеграция всех модулей и тестирование API (все)

**Неделя 4:** 
- Тестирование на реальных проходах
- Настройка логирования и мониторинга
- Деплой на VM и финальная отладка

