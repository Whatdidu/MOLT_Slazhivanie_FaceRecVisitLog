# Logrotate конфигурация для Docker контейнеров Sputnik FaceID
# Установка: sudo cp docker-logs /etc/logrotate.d/sputnik-faceid

/var/lib/docker/containers/*/*.log {
    rotate 7
    daily
    compress
    delaycompress
    missingok
    notifempty
    copytruncate
    maxsize 100M
}
